// Your Amazon Associates tracking ID (e.g. 'powerrank0a-20'). This is the only place it needs
// to be set: every product link on the site re-points automatically, and the Associates
// disclosure on every page switches on. While it's empty, pages say the site is NOT affiliated.
const AFFILIATE_TAG = 'powerrank-20';
